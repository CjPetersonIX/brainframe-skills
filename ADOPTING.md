# Adopting these skills

Plain `SKILL.md`. Not the OS.

Claude Code: `~/.claude/skills/<name>/SKILL.md`

```bash
curl -fsSL https://raw.githubusercontent.com/CjPetersonIX/brainframe-skills/main/install.sh | bash
```

Other CLIs: paste the body under the frontmatter into rules, or keep the file and tell the agent to follow it.
